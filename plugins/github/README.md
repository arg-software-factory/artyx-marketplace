# GitHub token

Create a GitHub personal access token and provide it as `GITHUB_TOKEN` when prompted.

## Steps

1. Create a GitHub personal access token with the repository access needed for your workflow.
2. Paste the token into Artyx when prompted for `GITHUB_TOKEN`.

Docs: https://github.com/github/github-mcp-server#readme
