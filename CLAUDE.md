# Artyx Marketplace

Follow `AGENTS.md`. This repository is a minimal catalog: package payload under
`plugins/` or `agents/`, maintainer-only implementation under `tooling/`, and no
runtime executables or session memory.
