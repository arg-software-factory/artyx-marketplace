# Artyx Marketplace

Curated plugins for [Artyx](https://artyx.ai).

Each plugin lives in `plugins/<name>/` with an `.artyx-plugin/plugin.json`
manifest. The marketplace index is `.agents/plugins/marketplace.json`.

Plugins may include `skills/`, `.mcp.json`, assets, and their own README.
