# Session 2026-07-17T1520 (claude) - Pure-client marketplace plugin cleanup

## Worked on
- Wave 1 pure-client cleanup on artyx-marketplace (branch feat/pure-client-cleanup)
- Remove bundled MCP servers; plugins connect only to external MCP via stdio or HTTP

## Changes
- Removed mcp-power-user plugin (playbook moves to artyx-desktop agent prompt)
- Blender v2.0.0: deleted bundled server; HTTP to official Lab MCP at http://127.0.0.1:${BLENDER_MCP_PORT}/mcp; skills/README updated for official tool surface
- Godot v2.0.0: deleted bundled server; stdio via npx -y @coding-solo/godot-mcp with GODOT_PATH=${GODOT_PATH}
- Unreal Engine: parametrized MCP host and port with ${VAR} placeholders
- Added CI plugin validator enforcing pure-client marketplace rules
- Documented pure-client plugin format in root README

## Decisions
- Marketplace main must stay compatible with current desktop: stdio {command,args,env} and http {type,url,headers?} only
- No new .mcp.json schema shapes (no per-platform overrides this wave)
- plugins/roblox-studio/ left unchanged (keeps launcher shim until desktop ships platform-overrides)

## Open threads
- feat/pure-client-cleanup branch has commits; PR/merge to main not shown in evidence
- Truncated task spec may include additional plugin updates not confirmed in commits
